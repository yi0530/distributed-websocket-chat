# CLAUDE.md

## 1. 项目定位

本仓库是《计算机网络》课程大作业项目：`distributed-websocket-chat`。

项目目标是实现一个基于 WebSocket 的分布式实时聊天系统，用于展示：

- WebSocket 实时通信；
- 自定义 JSON 应用层协议；
- JWT 登录认证；
- 群聊与私聊；
- 在线状态维护；
- ACK 与消息去重；
- Redis 离线消息缓存；
- Redis Pub/Sub 多节点消息转发；
- 网络系统测试与可观测性。

本项目是课程级工程，不是商业级 IM 系统。代码修改应优先保证：可运行、可解释、可测试、可展示。

------

## 2. Claude Code 工作原则

在本仓库中工作时，遵守以下原则：

1. 先阅读相关代码，再提出修改方案。
2. 修改前说明：
   - 相关文件；
   - 当前逻辑；
   - 问题判断；
   - 计划修改点；
   - 可能影响范围。
3. 修改后说明：
   - 修改了哪些文件；
   - 每个文件解决了什么问题；
   - 是否改变协议、Redis key、认证逻辑或前端状态结构；
   - 如何手动测试；
   - 是否存在未解决风险。
4. 优先小步、局部、可回滚修改。
5. 如果任务需要扩大范围，应先说明必要性、收益、风险和替代方案，再等待用户确认。
6. 不要为了让流程容易通过而删除真实业务约束或伪造实现。

------

## 3. 修改权限分级

本项目允许修改，但修改必须按影响范围分级处理。

### 3.1 绿区：可在说明计划后直接修改

以下修改通常属于低风险，可以在说明计划后直接执行：

- 修复明显 bug；
- 增加错误提示；
- 增加日志；
- 修复前端状态污染；
- 修复小范围异常处理；
- 补充手动测试说明；
- 增加轻量测试脚本；
- 重命名局部变量；
- 提取很小的重复逻辑，但不改变外部行为；
- 修复文档中与代码明显不一致的描述。

执行绿区修改时，仍需保持小步提交思路，不要混入无关改动。

### 3.2 黄区：必须先给方案，用户确认后修改

以下修改允许做，但必须先给出方案、影响范围和回滚方式，等待用户确认：

- 修改前端客户端状态结构；
- 修改 WebSocket 连接身份生命周期；
- 修改登录、断开、重连流程；
- 修改离线消息补发时机；
- 修改 ACK 或消息去重策略；
- 新增 service 文件；
- 调整模块依赖关系；
- 修改 Redis 数据结构或 key 设计；
- 修改 JSON 应用层协议字段；
- 增加注册功能；
- 增加自动重连；
- 增加压测脚本；
- 增加课程报告所需的证据日志或调试面板；
- 对多个文件进行联动修改。

黄区修改不是禁止项。只要用户确认，可以执行。

### 3.3 红区：必须获得明确授权后才能执行

以下属于高风险修改，未经用户明确授权不得执行：

- 替换当前 WebSocket 服务框架；
- 将主业务服务迁移到手写 RFC6455 WebSocket 实现；
- 重写后端入口；
- 重写整个前端；
- 重建项目目录结构；
- 删除核心模块；
- 更换 Redis 为其他存储；
- 引入大型框架或大型依赖；
- 把项目改造成另一个技术栈；
- 伪造压测结果、抓包结果或课程报告结论；
- 为了演示效果删除认证、ACK、去重、权限校验等真实业务约束。

红区并不代表永远不能做。如果用户明确要求并确认方案，可以执行。

------

## 4. 当前任务与长期规则的关系

本文件只描述长期项目规则，不描述当前具体任务。

如果存在以下文件，应优先读取它们来理解当前任务：

- `docs/agent/current-task.md`
- `.claude/tasks/current-task.md`
- `.claude/rules/*.md`
- `.claude/skills/*/SKILL.md`

不要把本文件中的长期原则误解为当前任务清单。

当前任务必须由用户在对话中明确指定，或由当前任务文件指定。

------

## 5. 协议兼容性规则

本项目已有自定义 JSON 应用层协议。

修改协议相关代码时，必须先确认是否影响：

- `msg_type`
- `msg_id`
- `payload`
- `ack`
- `timestamp`
- 登录消息格式
- 群聊消息格式
- 私聊消息格式
- 错误响应格式
- 前端收发逻辑
- 报告中的协议说明

如果需要修改协议，必须同步修改：

1. 后端解析；
2. 后端校验；
3. 前端发送；
4. 前端接收；
5. 错误处理；
6. 测试说明；
7. 协议文档或变更说明。

不能只改一端。

------

## 6. WebSocket 分层规则

处理 WebSocket 相关问题时，必须区分两层：

### 6.1 WebSocket 协议层

包括：

- HTTP Upgrade；
- RFC6455 frame；
- 文本帧；
- 二进制帧；
- Ping/Pong frame；
- Close frame；
- mask/unmask。

### 6.2 应用层协议

包括：

- JSON 消息；
- `msg_type`；
- `msg_id`；
- 应用层 heartbeat；
- ACK；
- 登录；
- 群聊；
- 私聊；
- 离线消息；
- Redis Pub/Sub 转发。

不要混淆这两层。

如果当前代码使用 WebSocket 库完成协议层，不要声称项目手写了 RFC6455 握手和帧解析。

如果用户要求手写 RFC6455，必须先给出迁移方案、影响范围和兼容策略，确认后再执行。

------

## 7. 连接身份生命周期原则

处理后端连接、登录、在线状态时，应遵守：

1. 一个 WebSocket 连接在任意时刻应有清晰的身份状态。
2. 已认证连接是否允许重新 login，必须有明确策略。
3. 不应在无清理逻辑的情况下静默覆盖 `ctx.user_id`。
4. 断开连接时，应清理该连接相关的心跳任务、在线续约任务和连接上下文。
5. Redis 在线状态清理应避免误删其他节点或其他连接的在线状态。
6. 离线消息补发应发生在用户身份明确之后。
7. 多连接、多用户、多节点场景下的影响必须说明。

------

## 8. 前端状态管理原则

处理前端调试页时，应遵守：

1. 客户端 A/B 的连接状态应隔离。
2. token、当前用户、room conversation_id、private conversation_id 不应跨客户端污染。
3. 切换账号、断开连接、重新登录时，应有明确状态清理策略。
4. 发送群聊或私聊时，应使用当前客户端对应的 conversation_id。
5. 如果后端返回会话归属错误，前端应明确提示，不应表现为无响应。
6. 前端调试台应尽量显示原始收发 JSON，方便调试和答辩。
7. UI 可以简洁，但状态和错误必须可见。

------

## 9. Redis 与消息可靠性原则

处理 Redis、离线消息、ACK、去重、Pub/Sub 时，应遵守：

1. 不要随意改变 Redis key 前缀。
2. 修改 Redis 数据结构前必须说明迁移影响。
3. 去重逻辑必须和 `msg_id` 语义保持一致。
4. 离线消息删除策略必须考虑消息丢失风险。
5. Pub/Sub 跨节点转发和本地连接投递应区分清楚。
6. ACK、离线消息、去重三者之间的关系必须能解释清楚。
7. 不要为了测试方便删除可靠性约束。

------

## 10. 可观测性与错误处理原则

本项目用于课程展示，错误和状态应尽量可见。

优先保证：

- 登录失败有明确错误；
- 未认证访问有明确错误；
- 非法消息有明确错误；
- 会话不存在有明确错误；
- 用户不属于会话有明确错误；
- Redis 异常有日志；
- WebSocket 异常断开有日志；
- 前端能看到关键发送、接收和错误信息。

不要静默吞掉异常。
不要让前端表现为“没反应”。
不要只返回模糊失败信息。

------

## 11. 测试要求

每次修改后，必须给出最小验证方式。

根据修改内容，测试说明应覆盖相关项：

- 正常登录；
- 未认证消息拦截；
- 重复 login；
- 断开重连；
- 客户端 A/B 状态隔离；
- 群聊创建、加入、发送；
- 私聊创建、发送；
- 接收方离线后补发；
- 重复 `msg_id` 去重；
- Redis Pub/Sub 跨节点转发；
- 错误场景前端提示。

不要求每次都覆盖全部场景，但必须覆盖本次修改影响到的场景。

------

## 12. 课程报告真实性原则

后续报告和答辩必须基于真实实现。

不要虚构：

- 未实现的 RFC6455 手写协议层；
- 未执行的压测数据；
- 未抓包验证的结论；
- 未实现的注册系统；
- 未实现的 ACK 重传；
- 未验证的分布式行为。

如果某能力由库完成，应明确说明由库完成。
如果某能力是项目手写实现，应能指出代码位置和测试证据。
如果某能力只是后续计划，应标记为后续改进。

------

## 13. 最重要的原则

允许修改。
允许重构。
允许新增功能。
允许为完成课程要求进行较大调整。

但任何修改都必须满足：

> 先说明必要性，再说明影响范围；小改直接做，大改先确认；实现必须真实，系统必须可运行，结论必须可验证。